<%= render "layouts/errors", :object => @product %>

<div class="container">
  <h1>New review for <%= @product.name %></h1>
  <%= render 'form' %>

</div>

<p><%= link_to "Return to product", product_path(@review.product) %></p>
